// Package value provides the transport structures
// used to unmarshal the config value types like string patterns,
// integer ranges, etc. from config files
// and convert them into the matching domain values
// through their Config methods.
package value
